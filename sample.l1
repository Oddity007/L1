__declare map;
map f [] = [];
id x = x;
map f [x ... xs] = [f x ... map f xs];
__declare any;
any f [x ... xs] = (f x | any f xs);
monad m = 
(
	(
		a b ->
		(
			m.unit :: m a;
			m.bind :: (($m $a) ($a -> m b) -> m b);
			m
		)
	) :: (a b -> m);
	m
);
monad